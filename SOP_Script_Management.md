# 剧本管理 SOP (标准作业程序)

本文档记录了 `血染工具/public/juben` 目录下剧本文件的**去重**和**索引构建**的标准流程。

## 核心逻辑说明

### 1. 剧本指纹 (Fingerprint)
我们将“角色配置完全相同”的剧本视为同一个剧本，忽略文件名、剧本名、作者等差异。
- **指纹计算规则**：提取剧本中所有 `townsfolk` (镇民), `outsider` (外来者), `minion` (爪牙), `demon` (恶魔) 的名称。
- **去重逻辑**：将这些名称放入一个集合 (Set) 并排序，生成唯一的指纹标识。
- **兼容性**：支持 List-of-Dicts (标准) 和 Dict-of-Lists (部分旧格式) 两种 JSON 结构。

### 2. 评分机制 (Scoring)
当发现多个文件具有相同指纹时，我们通过评分来决定保留哪一个（去重时）或使用哪一个的元数据（构建索引时）。
- **Logo 有效性 (+1000分)**：通过 HTTP HEAD 请求检查 `logo` URL 是否返回 200 OK。
- **内容完整性 (+10分/项)**：包含 `travelers` (传奇角色) 或 `fabled` (寓言角色) 的剧本得分更高。

---

## 流程一：文件夹内去重 (Deduplication)

**目的**：清理每个子文件夹内部的重复剧本，只保留质量最高的一个版本。

**脚本**：`deduplicate_scripts.py`

**执行步骤**：
1. 运行脚本：
   ```bash
   python3 deduplicate_scripts.py
   ```
2. 输入目标目录（默认为 `public/juben`）。
3. 脚本会自动扫描每个子文件夹，发现指纹相同的剧本时，保留得分最高的，删除其他的。

---

## 流程二：构建全局索引 (Build Index)

**目的**：生成 `public/scripts.json`，供前端应用读取。

**脚本**：`build_index_final.py`

**执行步骤**：
1. 运行脚本：
   ```bash
   python3 build_index_final.py
   ```
2. **逻辑说明**：
   - **跨文件夹聚合**：脚本会扫描所有文件夹。如果同一个剧本（指纹相同）出现在多个文件夹中（例如 `官方/BotC.json` 和 `推荐/BotC.json`），它们会被合并为一条索引记录。
   - **Types 字段**：该记录的 `types` 字段会包含所有来源文件夹的名称（如 `["官方", "推荐"]`）。
   - **元数据选取**：索引中的 `name`, `author`, `logo` 等字段取自所有副本中评分最高的文件。
   - **Logo 处理**：必须包含 `logo` 字段。如果最高分文件的 logo URL 有效，则存储该 URL；否则存为 `null`。
   - **Path 处理**：`path` 字段指向评分最高的文件路径（相对路径，如 `/public/juben/...`）。

---

## 附录：文件命名规范

为确保在所有平台和浏览器中都能正常访问，剧本文件名**严禁包含**以下字符：
- `#` (井号) - 会被浏览器截断为锚点
- `?` (问号)
- `&` (和号)
- 空格 (建议使用 `_` 或 `-` 代替)

如果在去重或整理过程中发现此类文件，请重命名（例如将 `#` 替换为 `_`）。

## 附录：文件格式要求

剧本 JSON 文件应符合以下结构之一：

**格式 A (推荐)**：
```json
[
  {
    "id": "_meta",
    "name": "剧本名",
    "author": "作者",
    "logo": "http://..."
  },
  {
    "id": "role_id",
    "name": "角色名",
    "team": "townsfolk"
  },
  ...
]
```

**格式 B**：
```json
{
  "_meta": { ... },
  "townsfolk": [ ... ],
  "outsider": [ ... ],
  ...
}
```
