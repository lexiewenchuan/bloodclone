# 版本管理与回滚指南

本项目已配置完整的版本管理流程，确保你可以随时回滚代码，并保证 GitHub 和 Vercel 的部署安全。

## 🚀 快速开始

我们在 `package.json` 中配置了以下快捷命令，你可以直接在 Trae 终端中运行：

| 命令 | 说明 | 适用场景 | 安全性 |
| :--- | :--- | :--- | :--- |
| `npm run save` | 提交并推送到 GitHub | 日常开发，保存进度 | ✅ 安全 |
| `npm run release` | 发布新版本 (自动打标签) | 准备部署稳定版时 | ✅ 安全 |
| `npm run versions` | 查看已发布版本 | 想知道有哪些正式版 Tag | ✅ 安全 |
| `npm run undo` | 撤销上一次提交 | 刚提交完发现有错，想撤回 | ✅ 安全 (推荐) |
| `npm run reset` | 强制回退到上一版 | 彻底放弃最新修改，不留记录 | ⚠️ 危险 |
| `npm run history` | 查看提交历史 | 想知道有哪些提交记录可以回退 | ✅ 安全 |
| `npm run goto <id>` | 强制回退到指定版本 | 想直接跳回某个特定的旧版本 | ⚠️ 危险 |

---

## 📸 核心概念：版本是如何诞生的？

这是最重要的一点：**Git 不会自动保存版本。**

这就好比你在写文档，虽然你在打字，但如果你不按 `Ctrl+S` (保存)，内容就不会被存盘。
在我们的项目中，**`npm run save` 就相当于那个“保存”按钮**。

### 触发机制
1.  **修改阶段 (草稿)**: 
    - 你在 Trae 中修改了代码，保存了文件。
    - 此时，Git 知道文件变了，但它认为这只是“草稿”，**还没有生成版本号**。
    - 如果你现在把电脑关了，文件还在，但 Git 历史里没有这次修改的记录。

2.  **提交阶段 (定格)**:
    - **当你运行 `npm run save` 时**，脚本会做两件事：
        1.  `git add .`: 把所有修改过的文件放到“待提交区”。
        2.  `git commit`: **咔嚓！** 拍一张快照。
    - **就在这一瞬间，一个新的版本号（如 `b2c3d4e`）诞生了。**
    - 只有生成了版本号，你以后才能通过 `npm run goto b2c3d4e` 回到来。

**总结**: 
- 改代码 ≠ 新版本
- 保存文件 ≠ 新版本
- **运行 `npm run save` = 新版本**

---

## 🧐 理解本地版本

你说得非常对！Git 的核心机制就是：**所有版本都存储在本地**。

当你想回滚远程代码时，操作流程确实是：
1.  **本地回退**: 先把本地的工作目录恢复到旧版本的状态。
2.  **推送到远程**: 把这个“旧状态”推送到 GitHub/Vercel，覆盖掉那个“不满意的新版本”。

我们为你提供了两个新工具来可视化和操作这个过程：

### 1. 查看历史 (`npm run history`)
运行此命令，你会看到类似这样的列表：
```text
a1b2c3d - 2024-02-18 14:30 - 修复了登录页面的样式问题 <User>
9z8y7x6 - 2024-02-18 12:00 - 添加了用户注册功能 <User>
...
```
- 最前面黄色的 `a1b2c3d` 就是 **Commit ID (版本号)**。
- 你可以根据时间和描述找到你想回到的那个版本。

### 2. 回退到指定版本 (`npm run goto`)
如果你发现 `a1b2c3d` 这个版本有问题，你想直接回到 `9z8y7x6` (即注册功能刚做好的时候)：

**用法**: `npm run goto 9z8y7x6`

**发生了什么**:
1.  本地代码瞬间变回 `9z8y7x6` 时的样子。
2.  `a1b2c3d` 以及之后的所有修改都会被**永久丢弃**。
3.  自动强制推送到远程，GitHub 和 Vercel 也会回到这个状态。

---

## 📖 详细使用说明

### 1. 日常提交 (`npm run save`)
每次完成一个小功能或修改，都应该运行此命令。
- **用法**: 
  - 运行 `npm run save`，然后按提示输入提交信息。
  - 或者直接带参数: `npm run save -- "修复了登录页面的样式问题"`
- **效果**: 代码会同步到 GitHub `main` 分支，Vercel 会自动触发构建预览。

### 2. 发布版本 (`npm run release`)
当你觉得当前代码已经稳定，可以发布给用户使用时。
- **用法**: `npm run release`
- **效果**: 
  - 自动生成基于日期的版本号 (如 `v20240218-1430`)。
  - 在 Git 中创建一个 Tag。
  - 推送到 GitHub。
  - **Vercel 也会为这个 Tag 进行独立部署**，这非常重要，因为 Tag 的部署不会被后续的 commit 覆盖。

### 3. 回滚方案 A: 撤销最近一次提交 (`npm run undo`)
如果你刚推送了一个 commit，发现导致了严重 Bug，这是最安全的“后悔药”。
- **用法**: `npm run undo`
- **原理**: 执行 `git revert HEAD`。它不会删除历史记录，而是创建一个**新的提交**，内容是把上一次的修改反向改回去。
- **优点**: 历史记录清晰，不会造成多人协作冲突。Vercel 会自动部署这个“修复版”。

### 4. 回滚方案 B: 强制回退 (`npm run reset`)
**⚠️ 慎用**。如果你想让代码库看起来就像从来没写过上一次代码一样。
- **用法**: `npm run reset` (会有确认提示)
- **原理**: `git reset --hard HEAD^` 并且 `git push --force`。
- **缺点**: 如果其他人已经拉取了代码，这会导致冲突。但在个人项目中这是最彻底的回退。

---

## 🌐 在 Vercel 上回滚 (终极防线)

即使 Git 操作出了问题，Vercel 平台本身也提供了强大的回滚能力。

1. **Instant Rollback (即时回滚)**
   - 打开 Vercel Dashboard -> 进入项目 -> **Deployments** 标签页。
   - 找到上一个显示 ✅ Ready 的绿色部署记录。
   - 点击右侧的三个点 `...` -> 选择 **Instant Rollback**。
   - Vercel 会立即将流量切换回该版本，无需重新构建，速度极快。

2. **部署特定 Tag**
   - 如果你使用了 `npm run release`，Vercel 会保留所有 Tag 的部署。
   - 你可以在 Deployments 列表中通过 Filter 筛选 Tag，找到对应的稳定版链接。

## 💡 最佳实践建议

1. **勤提交**: 不要写了一整天代码才提交一次。建议每完成一个功能点就 `npm run save`。
2. **多打标**: 每次觉得“这个版本挺好”的时候，就 `npm run release`。
3. **先撤销**: 遇到问题优先用 `npm run undo`，而不是 `npm run reset`。
